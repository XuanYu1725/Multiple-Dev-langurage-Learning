// using gcc and ./Target commands to compile and run a simple C program
// to compile: 'gcc Target.c -o Target' or 'g++ Target.c -o Target'
// to run: ./Target
// This program prints "Hello World!" to the console

#include<stdio.h>

int main(){
    printf("Hello World!\n");
    return 0;
}